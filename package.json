{
  "name": "google-review-generator",
  "version": "1.0.0",
  "description": "Auto create google account and generate review using cypress.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress": "cypress open",
    "start-ci": "set CI=1 && set DEBUG=cypress:* && cypress run"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nathanberron/google-review-generator.git"
  },
  "homepage": "https://github.com/nathanberron/google-review-generator#readme",
  "dependencies": {
    "cypress": "^8.6.0",
    "faker": "^5.5.3"
  }
}